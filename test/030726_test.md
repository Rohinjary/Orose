cat << 'MDEOF' > /mnt/user-data/outputs/test_final_orose.md
# PLAN DE TEST FINAL — OROSE
**Date de test : 03/07/2026**
Utilisateur connecté : `admin@baovola.mg` / `admin123`

---

## ÉTAPE 0 — Vérification de l'état initial

```sql
-- Vérifie que les données de référence sont bien en place
SELECT * FROM statut_bassin ORDER BY id;
SELECT * FROM creneau_horaire ORDER BY ordre;
SELECT * FROM espece_crevette;
SELECT * FROM evolution_hebdo_espece ORDER BY semaine;
SELECT * FROM utilisateur;
SELECT * FROM role;
SELECT * FROM aliment;
SELECT * FROM entree_stock_aliment;
```

**Attendu :** 6 statuts (VIDE, PREPARATION, ACTIF, EN_TRAITEMENT, RECOLTE, QUARANTAINE + INACTIF),
4 créneaux (MATIN=1, MIDI=2, SOIR=3, NUIT=4),
1 espèce (Fenneropenaeus indicus),
16 semaines dans evolution_hebdo_espece,
1 utilisateur Admin,
1 aliment (Granulés Croissance Élevée),
3 lots stock aliment (id 101, 102, 103).

---

## ÉTAPE 1 — MODULE 1 : CRÉER LES 9 BASSINS

> **Via interface** — formulaire Nouveau Bassin

| # | Code | Surface (m²) | Profondeur (m) | Notes |
|---|------|-------------|----------------|-------|
| 1 | B01 | 500.00 | 1.20 | — |
| 2 | B02 | 500.00 | 1.20 | — |
| 3 | B03 | 450.00 | 1.10 | — |
| 4 | B04 | 450.00 | 1.10 | — |
| 5 | B05 | 600.00 | 1.30 | — |
| 6 | B06 | 600.00 | 1.30 | — |
| 7 | B07 | 400.00 | 1.00 | Bassin témoin |
| 8 | B08 | 400.00 | 1.00 | — |
| 9 | B09 | 550.00 | 1.15 | — |

**Statut initial attendu : VIDE** (c'est le premier statut dans ta table, id=1).

**Vérification :**
```sql
SELECT id, code, surface_m2, profondeur_metre FROM bassin ORDER BY code;
-- Attendu : 9 lignes, toutes à statut VIDE
```

---

## ÉTAPE 2 — MODULE 1 : PASSER LES 6 BASSINS EN PRÉPARATION

> **Via interface** — Changement d'état sur chaque bassin (VIDE → PREPARATION)

Bassins à passer en PREPARATION : **B01, B02, B03, B04, B05, B06**

B07, B08, B09 restent en VIDE (non utilisés dans ce test).

**Vérification :**
```sql
SELECT b.code, sb.code AS statut
FROM bassin b JOIN statut_bassin sb ON sb.id = b.id_statut_actuel
ORDER BY b.code;
-- B01-B06 = PREPARATION, B07-B09 = VIDE
```

---

## ÉTAPE 3 — MODULE 1 : DÉMARRER 2 CYCLES

> **Via interface** — Formulaire Nouveau Cycle

### Cycle 1 — C01 (bassins B01, B02, B03)

| Champ | Valeur |
|---|---|
| Code unique cycle | C01 |
| Espèce | Crevette blanche |
| Date début | 03/07/2026 |
| Date fin prévue | 03/11/2026 *(+4 mois)* |
| Technicien | Admin OROSE |

**Bassins à associer à C01 :**

| Bassin | Effectif initial | Densité m² | Coût post-larves (Ar) |
|---|---|---|---|
| B01 | 50 000 | 100.00 | 750 000.00 |
| B02 | 50 000 | 100.00 | 750 000.00 |
| B03 | 45 000 | 100.00 | 675 000.00 |

### Cycle 2 — C02 (bassins B04, B05, B06)

| Champ | Valeur |
|---|---|
| Code unique cycle | C02 |
| Espèce | Crevette blanche |
| Date début | 03/07/2026 |
| Date fin prévue | 03/11/2026 |
| Technicien | Admin OROSE |

**Bassins à associer à C02 :**

| Bassin | Effectif initial | Densité m² | Coût post-larves (Ar) |
|---|---|---|---|
| B04 | 45 000 | 100.00 | 675 000.00 |
| B05 | 60 000 | 100.00 | 900 000.00 |
| B06 | 60 000 | 100.00 | 900 000.00 |

**Vérification :**
```sql
SELECT c.code_unique_cycle, b.code AS bassin, cba.effectif_initial,
       cba.densite_m2, cba.cout_post_larves, cba.est_cloture
FROM cycle c
JOIN cycle_bassin_assoc cba ON cba.id_cycle = c.id
JOIN bassin b ON b.id = cba.id_bassin
ORDER BY c.code_unique_cycle, b.code;

-- Statuts bassins après démarrage cycle (doivent être ACTIF) :
SELECT b.code, sb.code AS statut
FROM bassin b JOIN statut_bassin sb ON sb.id = b.id_statut_actuel
WHERE b.code IN ('B01','B02','B03','B04','B05','B06');
```

---

## ÉTAPE 4 — MODULE 2 : SAISIR LES PESÉES

> **Via terminal** — Ces INSERT supposent que `cycle_bassin_assoc` a les ids suivants.
> **Vérifie d'abord :**
```sql
SELECT cba.id, b.code, c.code_unique_cycle
FROM cycle_bassin_assoc cba
JOIN bassin b ON b.id = cba.id_bassin
JOIN cycle c ON c.id = cba.id_cycle
ORDER BY c.code_unique_cycle, b.code;
```
> Si tes ids sont différents de 1-6, adapte les valeurs `id_cycle_bassin_assoc` ci-dessous.

```sql
-- id_technicien = 1 = Admin (adapter si différent)

-- BASSIN B01 / C01 (id_cycle_bassin_assoc = 1)
-- Croissance normale 0g -> 20g sur 16 semaines
INSERT INTO suivi_hebdo_bassin
  (id_cycle_bassin_assoc, date_suivi, semaine_actuelle,
   poids_moyen_gramme, taille_moyenne_mm, nb_vivants, nb_morts, id_technicien, notes)
VALUES
  (1, '2026-07-10',  1,  0.50,   8.00, 49800,  200, 1, 'S1 - début cycle'),
  (1, '2026-07-31',  4,  2.80,  25.00, 49200,  600, 1, 'S4 - croissance normale'),
  (1, '2026-08-28',  8,  8.50,  55.00, 48500,  700, 1, 'S8 - mi-cycle'),
  (1, '2026-09-25', 12, 14.50,  85.00, 47800,  700, 1, 'S12 - approche calibre'),
  (1, '2026-10-23', 16, 20.00, 120.00, 47000,  800, 1, 'S16 - calibre atteint, récolte possible');

-- BASSIN B02 / C01 (id_cycle_bassin_assoc = 2)
INSERT INTO suivi_hebdo_bassin
  (id_cycle_bassin_assoc, date_suivi, semaine_actuelle,
   poids_moyen_gramme, taille_moyenne_mm, nb_vivants, nb_morts, id_technicien, notes)
VALUES
  (2, '2026-07-10',  1,  0.50,   8.00, 49700,  300, 1, 'S1'),
  (2, '2026-08-14',  6,  5.50,  40.00, 48900,  800, 1, 'S6'),
  (2, '2026-09-11', 10, 11.50,  70.00, 48200,  700, 1, 'S10'),
  (2, '2026-10-09', 14, 17.50, 105.00, 47500,  700, 1, 'S14 - proche calibre');

-- BASSIN B03 / C01 (id_cycle_bassin_assoc = 3)
-- Mortalité élevée volontaire pour tester les alertes
INSERT INTO suivi_hebdo_bassin
  (id_cycle_bassin_assoc, date_suivi, semaine_actuelle,
   poids_moyen_gramme, taille_moyenne_mm, nb_vivants, nb_morts, id_technicien, notes)
VALUES
  (3, '2026-07-10', 1, 0.50, 8.00, 44800,  200, 1, 'S1'),
  (3, '2026-08-07', 5, 4.00, 32.00, 43000, 1800, 1, 'S5 - mortalité élevée à signaler');

-- BASSIN B04 / C02 (id_cycle_bassin_assoc = 4)
INSERT INTO suivi_hebdo_bassin
  (id_cycle_bassin_assoc, date_suivi, semaine_actuelle,
   poids_moyen_gramme, taille_moyenne_mm, nb_vivants, nb_morts, id_technicien, notes)
VALUES
  (4, '2026-07-10',  1,  0.50,   8.00, 44700,  300, 1, 'S1'),
  (4, '2026-07-31',  4,  2.80,  25.00, 44100,  600, 1, 'S4'),
  (4, '2026-08-28',  8,  8.50,  55.00, 43500,  600, 1, 'S8'),
  (4, '2026-09-25', 12, 14.50,  85.00, 42900,  600, 1, 'S12'),
  (4, '2026-10-23', 16, 20.00, 120.00, 42200,  700, 1, 'S16 - calibre atteint');

-- BASSIN B05 / C02 (id_cycle_bassin_assoc = 5)
INSERT INTO suivi_hebdo_bassin
  (id_cycle_bassin_assoc, date_suivi, semaine_actuelle,
   poids_moyen_gramme, taille_moyenne_mm, nb_vivants, nb_morts, id_technicien, notes)
VALUES
  (5, '2026-07-10', 1, 0.50,  8.00, 59700, 300, 1, 'S1'),
  (5, '2026-08-14', 6, 5.50, 40.00, 58800, 900, 1, 'S6');

-- BASSIN B06 / C02 (id_cycle_bassin_assoc = 6)
INSERT INTO suivi_hebdo_bassin
  (id_cycle_bassin_assoc, date_suivi, semaine_actuelle,
   poids_moyen_gramme, taille_moyenne_mm, nb_vivants, nb_morts, id_technicien, notes)
VALUES
  (6, '2026-07-10',  1,  0.50,   8.00, 59800,  200, 1, 'S1'),
  (6, '2026-08-28',  8,  8.50,  55.00, 58600, 1200, 1, 'S8 - mortalité à surveiller'),
  (6, '2026-10-09', 14, 17.50, 105.00, 57800,  800, 1, 'S14'),
  (6, '2026-10-23', 16, 20.00, 120.00, 57500,  300, 1, 'S16 - calibre atteint');
```

**Vérification — biomasse calculée automatiquement :**
```sql
SELECT id_cycle_bassin_assoc, date_suivi, semaine_actuelle,
       poids_moyen_gramme, nb_vivants, biomasse_calculee_kg,
       taux_survie_calcule, taux_mortalite_calcule
FROM v_suivi_hebdo_bassin
ORDER BY id_cycle_bassin_assoc, date_suivi;
```

**Attendu :**
- B01/S16 : biomasse = 47000 × 20 / 1000 = **940.00 kg**
- B04/S16 : biomasse = 42200 × 20 / 1000 = **844.00 kg**
- B06/S16 : biomasse = 57500 × 20 / 1000 = **1150.00 kg**
- B03/S5 : taux_mortalite_calcule = 1800/45000 × 100 = **4.00%** → doit déclencher une alerte

**Vérification alerte :**
```sql
SELECT * FROM alerte ORDER BY date_creation DESC;
```

---

## ÉTAPE 5 — MODULE 1 : CHANGEMENTS D'ÉTAT (cas de test)

> **Via interface** — Changement d'état depuis la page détail bassin

### Test EN_TRAITEMENT (B05)

| Étape | Bassin | Transition | Motif |
|---|---|---|---|
| 1 | B05 | ACTIF → EN_TRAITEMENT | "pH anormal détecté à l'œil" |
| 2 | B05 | EN_TRAITEMENT → ACTIF | "Problème résolu après aération" |

**Vérification :**
```sql
SELECT id_bassin, id_statut_bassin, motif, date_changement
FROM histo_statut_bassin WHERE id_bassin = (SELECT id FROM bassin WHERE code='B05')
ORDER BY date_changement;
-- Doit afficher 2 lignes : passage en EN_TRAITEMENT puis retour ACTIF
```

### Test QUARANTAINE manuelle (B03) — avant le test sanitaire automatique

| Étape | Bassin | Transition | Motif |
|---|---|---|---|
| 1 | B03 | ACTIF → QUARANTAINE | "Mortalité élevée constatée lors de la pesée S5" |

**Test de blocage :** tenter de récolter B03 depuis l'interface → **doit être refusé**.

---

## ÉTAPE 6 — MODULE 3 : NOURRISSAGE

### 6.1 Vérification du stock aliment initial

```sql
SELECT id, id_aliment, quantite_kg, quantite_restante_kg,
       prix_unitaire_ar, date_reception, date_expiration
FROM entree_stock_aliment
ORDER BY date_expiration ASC;
-- Lot 101 : expire 01/09/2026 (premier à consommer)
-- Lot 102 : expire 31/12/2026
-- Lot 103 : expire 15/03/2027 (dernier)
```

### 6.2 Générer le planning du jour (aujourd'hui = 03/07/2026)

> **Via interface** — Dashboard nourrissage → le planning se génère automatiquement
> **Ou via terminal :**

```sql
SELECT * FROM fn_obtenir_ou_creer_planning_du_jour(1);
-- Doit retourner 4 créneaux × 6 bassins actifs = 24 lignes
-- Statut initial = EN_ATTENTE pour tous
```

**Vérification du planning créé :**
```sql
SELECT b.code AS bassin, ch.libelle AS creneau,
       dn.quantite_prevue_kg, dn.statut, dn.est_valide
FROM distribution_nourriture dn
JOIN cycle_bassin_assoc cba ON cba.id = dn.id_cycle_bassin_assoc
JOIN bassin b ON b.id = cba.id_bassin
JOIN creneau_horaire ch ON ch.id = dn.id_creneau
WHERE dn.date_distribution = '2026-07-03'
ORDER BY b.code, ch.ordre;
```

### 6.3 Cas de test — validations et blocages

> **IMPORTANT :** ces tests dépendent de l'heure actuelle du serveur.
> Pour les tests horaires, vérifier avec `SELECT CURRENT_TIME;`

**Cas 1 — Validation normale (MATIN, si heure ≥ 06:00)**
```sql
-- Récupérer l'id de la distribution MATIN de B01
SELECT dn.id, b.code, ch.libelle, dn.quantite_prevue_kg
FROM distribution_nourriture dn
JOIN cycle_bassin_assoc cba ON cba.id = dn.id_cycle_bassin_assoc
JOIN bassin b ON b.id = cba.id_bassin
JOIN creneau_horaire ch ON ch.id = dn.id_creneau
WHERE b.code = 'B01' AND ch.libelle = 'MATIN'
  AND dn.date_distribution = CURRENT_DATE;

-- Puis valider (remplacer 999 par l'id trouvé ci-dessus)
CALL pr_valider_nourrissage_direct(999, 1);
```
**Attendu :** statut → NOURRI, `distribution_nourriture_lot` créé, lot 101 décrémenté (expire en 1er = FEFO).

**Cas 2 — Blocage chronologie (MIDI avant MATIN validé)**
```sql
-- Tenter de valider le créneau MIDI de B02 sans que MATIN soit validé
-- Récupérer l'id MIDI de B02
SELECT dn.id FROM distribution_nourriture dn
JOIN cycle_bassin_assoc cba ON cba.id = dn.id_cycle_bassin_assoc
JOIN bassin b ON b.id = cba.id_bassin
JOIN creneau_horaire ch ON ch.id = dn.id_creneau
WHERE b.code = 'B02' AND ch.libelle = 'MIDI'
  AND dn.date_distribution = CURRENT_DATE;

CALL pr_valider_nourrissage_direct(999, 1); -- remplacer 999
```
**Attendu :** EXCEPTION "Impossible de valider le repas du MIDI car le repas précédent du MATIN n'a pas encore été validé"

**Cas 3 — Vérification FEFO après épuisement lot 101**
```sql
-- Voir la répartition FEFO après la première validation
SELECT dnl.id_distribution, ea.date_expiration, dnl.quantite_piquee_kg
FROM distribution_nourriture_lot dnl
JOIN entree_stock_aliment ea ON ea.id = dnl.id_entree_aliment
ORDER BY dnl.id_distribution, ea.date_expiration;
-- Le lot 101 (expire 01/09/2026) doit apparaître en premier
```

**Cas 4 — Blocage stock insuffisant**
```sql
-- Tenter une distribution manuelle avec une quantité > stock total disponible
-- Stock total = 100 + 200 + 150 = 450kg
CALL pr_enregistrer_distribution_manuelle('B03', 1, 500.00, 1, CURRENT_DATE, '06:30:00');
```
**Attendu :** EXCEPTION "Stock insuffisant... Requis : 500 kg, Disponible : 450 kg"

### 6.4 Ajouter un nouveau stock aliment

> **Via interface** OU via terminal :
```sql
CALL pr_enregistrer_entree_stock(
    (SELECT id FROM aliment WHERE libelle = 'Granulés Croissance Élevée'),
    300.00,    -- quantité kg
    2150.00,   -- prix unitaire Ar
    '2026-07-03'::DATE,  -- date réception
    '2027-06-30'::DATE,  -- date expiration
    1
);
```
**Vérification :**
```sql
SELECT id, quantite_kg, quantite_restante_kg, prix_unitaire_ar, date_expiration
FROM entree_stock_aliment ORDER BY date_expiration;
-- 4 lots maintenant, toujours triés par date expiration pour le FEFO
```

---

## ÉTAPE 7 — MODULE 4 : SANITAIRE

### 7.1 Créer le médicament et son stock

> **Via interface** — Formulaire nouveau médicament

| Champ | Valeur |
|---|---|
| Libelle | Oxytétracycline |
| Unité | kg |
| Seuil minimum | 5.00 |

> **Puis entrée stock médicament :**

| Champ | Lot 1 | Lot 2 |
|---|---|---|
| Médicament | Oxytétracycline | Oxytétracycline |
| Quantité | 20.00 | 10.00 |
| Prix total (Ar) | 400 000 | 200 000 |
| Date réception | 03/07/2026 | 03/07/2026 |
| Date expiration | 03/07/2027 | 03/01/2027 |

**Vérification :**
```sql
SELECT m.libelle, esm.id, esm.quantite, esm.quantite_restante, esm.date_expiration
FROM entree_stock_medicament esm
JOIN medicament m ON m.id = esm.id_medicament
ORDER BY esm.date_expiration;
-- Lot 2 expire avant Lot 1 -> sera consommé en premier (FEFO)
```

### 7.2 Déclarer un incident CRITIQUE sur B03

> **Via interface** — Formulaire nouvel incident

| Champ | Valeur |
|---|---|
| Bassin | B03 *(via id_cycle_bassin_assoc correspondant)* |
| Date détection | 03/07/2026 |
| Type incident | MORTALITE_ANORMALE |
| Description | Mortalité de 4% en une semaine, eau trouble |
| Niveau gravité | CRITIQUE |
| Responsable | Admin OROSE |

**Vérification automatisme quarantaine :**
```sql
-- Bassin B03 doit être passé automatiquement en QUARANTAINE
SELECT b.code, sb.code AS statut
FROM bassin b JOIN statut_bassin sb ON sb.id = b.id_statut_actuel
WHERE b.code = 'B03';

-- Historique doit avoir une ligne automatique
SELECT motif, date_changement
FROM histo_statut_bassin
WHERE id_bassin = (SELECT id FROM bassin WHERE code = 'B03')
ORDER BY date_changement;
-- La dernière ligne = "Quarantaine automatique - incident critique #X"
```

### 7.3 Enregistrer un traitement lié à l'incident B03

> **Via interface** — Formulaire nouveau traitement

| Champ | Valeur |
|---|---|
| Incident parent | Incident B03 du 03/07/2026 |
| Médicament | Oxytétracycline |
| Dosage | 2g / 100L d'eau |
| Durée | 7 jours |
| Date début | 03/07/2026 |
| Quantité utilisée | 3.00 |

**Vérification décrément stock médicament :**
```sql
SELECT esm.id, esm.quantite, esm.quantite_restante, esm.date_expiration
FROM entree_stock_medicament esm
JOIN medicament m ON m.id = esm.id_medicament
WHERE m.libelle = 'Oxytétracycline'
ORDER BY esm.date_expiration;
-- Le lot qui expire en premier (Lot 2, expire 03/01/2027) doit avoir 10.00 - 3.00 = 7.00 restants
-- (si 3kg pris sur 1 seul lot)
-- Vérifier aussi traitement_medicament_lot :
SELECT * FROM traitement_medicament_lot;
```

### 7.4 Test blocage stock médicament insuffisant

```sql
-- Tenter un traitement demandant 50kg alors que stock total = 20+7 = 27kg
-- Créer un incident sur B06 d'abord (niveau MODERE pour ne pas déclencher quarantaine)
INSERT INTO incident_sanitaire
  (id_cycle_bassin_assoc, date_detection, type_incident, description, niveau_gravite, id_responsable)
VALUES
  (6, CURRENT_DATE, 'AUTRE', 'Test blocage stock medicament', 'MODERE', 1);

-- Puis tenter un traitement impossible via interface avec quantite = 50kg
-- Attendu : EXCEPTION "Stock médicament insuffisant pour le lot ID X"
```

### 7.5 Résoudre l'incident B03 et lever la quarantaine

> **Via interface** — Marquer l'incident comme résolu

**Vérification :**
```sql
SELECT est_resolu FROM incident_sanitaire WHERE id_cycle_bassin_assoc = 3;
```

> Puis changer l'état B03 : QUARANTAINE → PREPARATION via l'interface.

**Test blocage : tenter la levée AVANT de résoudre** → doit être refusé (géré côté service Java).

---

## ÉTAPE 8 — MODULE 5 : STOCK & RÉCOLTE CREVETTES

### 8.1 Récolter B01 (poids 20g ≥ seuil 15g, taille 120mm ≥ 110mm)

> **Via interface** — Changement d'état B01 : ACTIF → RECOLTE

**Ce que l'application doit faire automatiquement :**
- Créer un lot crevette avec `numero_lot_unique = 'LOT-B01-2026'`
- `biomasse_totale_kg` = dernière pesée B01 = 940.00 kg
- `biomasse_actuelle_kg` = 940.00 kg
- `poids_moyen_final_g` = 20.00
- `taille_moyenne_finale_mm` = 120.00
- `cycle_bassin_assoc(B01).est_cloture = TRUE`

**Vérification :**
```sql
SELECT lc.numero_lot_unique, lc.biomasse_totale_kg, lc.biomasse_actuelle_kg,
       lc.poids_moyen_final_g, lc.date_recolte
FROM lot_crevette lc
JOIN cycle_bassin_assoc cba ON cba.id = lc.id_cycle_bassin_assoc
JOIN bassin b ON b.id = cba.id_bassin
WHERE b.code = 'B01';

SELECT est_cloture FROM cycle_bassin_assoc WHERE id = 1; -- doit être TRUE
SELECT sb.code FROM bassin b JOIN statut_bassin sb ON sb.id = b.id_statut_actuel WHERE b.code = 'B01';
-- doit être RECOLTE
```

### 8.2 Récolter B04

> **Via interface** — Changement d'état B04 : ACTIF → RECOLTE
> Biomasse attendue : 42200 × 20 / 1000 = 844.00 kg

```sql
SELECT lc.numero_lot_unique, lc.biomasse_totale_kg
FROM lot_crevette lc
JOIN cycle_bassin_assoc cba ON cba.id = lc.id_cycle_bassin_assoc
JOIN bassin b ON b.id = cba.id_bassin
WHERE b.code = 'B04';
```

### 8.3 Test mouvement stock crevette (perte)

> **Via interface** — Module Stock → Mouvement perte sur LOT-B01-2026

| Champ | Valeur |
|---|---|
| Lot | LOT-B01-2026 |
| Type mouvement | PERTE |
| Quantité (kg) | 15.00 |
| Motif | Casse pendant manipulation |

**Vérification trigger :**
```sql
SELECT numero_lot_unique, biomasse_totale_kg, biomasse_actuelle_kg
FROM lot_crevette WHERE numero_lot_unique = 'LOT-B01-2026';
-- biomasse_actuelle_kg = 940.00 - 15.00 = 925.00
```

### 8.4 Test blocage stock négatif

```sql
-- Tenter une perte de 2000kg sur LOT-B01-2026 (stock réel = 925kg)
INSERT INTO mouvement_stock_crevette
  (id_lot_crevette, type_mouvement, quantite_kg, motif, id_utilisateur)
VALUES
  ((SELECT id FROM lot_crevette WHERE numero_lot_unique = 'LOT-B01-2026'),
   'PERTE', 2000.00, 'Test blocage', 1);
-- Attendu : EXCEPTION "Stock crevette insuffisant pour ce mouvement"
```

### 8.5 Inventaire

> **Via interface** — Module Stock → Inventaire

| Type | Produit | Stock théorique | Stock réel constaté | Écart attendu |
|---|---|---|---|---|
| CREVETTE | LOT-B01-2026 | 925.00 | 920.00 | -5.00 |
| ALIMENT | Granulés (lot 101) | calculé auto | légèrement différent | auto |

**Vérification :**
```sql
SELECT type_produit, stock_theorique, stock_reel, ecart, date_inventaire
FROM inventaire ORDER BY date_inventaire DESC;
-- ecart = stock_reel - stock_theorique (colonne GENERATED ALWAYS)
```

---

## ÉTAPE 9 — MODULE 0 : SÉCURITÉ & JOURNAL

### 9.1 Vérifier le journal des actions

```sql
SELECT u.nom, j.module, j.action, j.entite_cible, j.date_heure
FROM journal_action j
JOIN utilisateur u ON u.id = j.id_utilisateur
ORDER BY j.date_heure DESC
LIMIT 20;
-- Doit afficher toutes les actions effectuées durant le test
```

### 9.2 Test blocage suppression utilisateur avec historique

> **Via interface** — Administration → tenter de supprimer Admin OROSE
> **Attendu :** refusé (géré côté service Java car l'utilisateur a des actions au journal).

### 9.3 Test désactivation utilisateur

> **Via interface** — Administration → désactiver l'utilisateur Admin → statut passe à INACTIF.
> Puis créer un second utilisateur et vérifier qu'on ne peut pas se connecter avec le compte désactivé.

```sql
-- Remettre Admin en ACTIF après le test
UPDATE utilisateur SET statut = 'ACTIF' WHERE email = 'admin@baovola.mg';
```

---

## RÉCAPITULATIF DES VÉRIFICATIONS FINALES

```sql
-- Vue d'ensemble complète après tous les tests
SELECT 'BASSINS' AS module, COUNT(*) AS total FROM bassin
UNION ALL
SELECT 'CYCLES', COUNT(*) FROM cycle
UNION ALL
SELECT 'CYCLE_BASSIN_ASSOC', COUNT(*) FROM cycle_bassin_assoc
UNION ALL
SELECT 'PESEES', COUNT(*) FROM suivi_hebdo_bassin
UNION ALL
SELECT 'DISTRIBUTIONS', COUNT(*) FROM distribution_nourriture
UNION ALL
SELECT 'DISTRIBUTION_LOTS', COUNT(*) FROM distribution_nourriture_lot
UNION ALL
SELECT 'INCIDENTS', COUNT(*) FROM incident_sanitaire
UNION ALL
SELECT 'TRAITEMENTS', COUNT(*) FROM traitement
UNION ALL
SELECT 'TRAITEMENT_LOTS', COUNT(*) FROM traitement_medicament_lot
UNION ALL
SELECT 'LOTS_CREVETTE', COUNT(*) FROM lot_crevette
UNION ALL
SELECT 'MVT_CREVETTE', COUNT(*) FROM mouvement_stock_crevette
UNION ALL
SELECT 'INVENTAIRES', COUNT(*) FROM inventaire
UNION ALL
SELECT 'ALERTES', COUNT(*) FROM alerte
UNION ALL
SELECT 'JOURNAL', COUNT(*) FROM journal_action;

-- Statuts finaux des bassins
SELECT b.code, sb.code AS statut
FROM bassin b JOIN statut_bassin sb ON sb.id = b.id_statut_actuel
ORDER BY b.code;
-- B01 = RECOLTE, B02 = ACTIF, B03 = PREPARATION (après levée quarantaine), 
-- B04 = RECOLTE, B05 = ACTIF, B06 = ACTIF, B07-B09 = VIDE
```
MDEOF

wc -l /mnt/user-data/outputs/test_final_orose.md

Sortie
644 /mnt/user-data/outputs/test_final_orose.md