package com.example.orose.repository;
import com.example.orose.model.EspeceCrevette;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EspeceCrevetteRepository extends JpaRepository<EspeceCrevette, Long> {
    
} 
